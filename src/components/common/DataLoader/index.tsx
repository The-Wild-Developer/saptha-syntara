import TowerLoader from "@/components/common/TowerLoader";

const DataLoader = () => {
  return (
    <div className="flex items-center justify-center py-16">
      <TowerLoader className="tower-loader--sm" />
    </div>
  );
};

export default DataLoader;
