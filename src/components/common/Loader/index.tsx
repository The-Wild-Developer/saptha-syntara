import TowerLoader from "@/components/common/TowerLoader";

const Loader = () => {
  return (
    <div className="flex h-screen items-center justify-center bg-white dark:bg-black">
      <TowerLoader />
    </div>
  );
};

export default Loader;
